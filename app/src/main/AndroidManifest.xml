<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="exchange.twex.twexapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InvestorRegistrationActivity"
            android:label="@string/title_activity_investor_registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".AboutUsScrollingActivity"
            android:label="@string/title_activity_about_us_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AMLScrollingActivity"
            android:label="@string/title_activity_amlscrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TAndCScrollingActivity"
            android:label="@string/title_activity_tand_cscrolling"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>